# 上游实时状态与 MCP 选择性整合

## 基线

- 本地分支：`integrate/upstream-main-20260714`
- 本地起点：`cc8e373`
- 上游候选：`5faf0811568dd8a2127c77bf17f993e5c67f7391`
- 策略：不直接 cherry-pick；只移植已验证且适合本地实现的行为与测试。

## 范围与约束

1. 接收 `thread_settings_applied` 后立即刷新 model、reasoning effort 和 service tier。
2. 将 MCP begin/end 事件关联为一条 `server/tool` 活动，覆盖增量读取边界。
3. 不保存 MCP 原始 arguments、stdout 或返回正文。
4. 保留本地现有 custom-tool 静态分析、脱敏和执行详情实现，不移植上游较弱的同类实现。
5. 不改动既有 `.codex/visualizations/`，本任务不创建 Git commit、不推送远端。

## 验证目标

- model/effort 在仅追加 `thread_settings_applied` 时即时更新。
- `function_call -> mcp_tool_call_end -> function_call_output` 只统计一次。
- 跨两次增量解析发生 MCP 重命名时，`callsByType` 从旧名称迁移到 `server/tool`。
- end-only MCP 事件仍可显示；错误状态和有效 duration 可恢复。
- 上游 custom-tool runtime 刷新测试在本地实现上通过。
- 完整构建、单元测试、集成测试、E2E、真实 rollout 回放和 `git diff --check` 通过。

## 进度

- [x] 核对上游提交、冲突面、现有本地扩展和真实 rollout 事件顺序。
- [x] 实现 thread settings 即时刷新。
- [x] 实现安全的 MCP 事件关联。
- [x] 补充并适配回归测试。
- [x] 完整验证与对抗性审查。

## 验证结果

- `npm run build`：通过。
- Unit：21/21 通过。
- Node integration：7/7 通过。
- Agent-status E2E：通过；完整套件首次运行有一次 5 秒画面等待超时，隔离复跑全部 transition 通过。
- Shell integration：15/15 通过；真实 tmux 用例显式设置 `SHELL=/bin/sh`，避免继承本机 zsh 启动脚本。
- Legacy E2E：10/10 通过。
- Bash 语法：22 个脚本通过。
- `git diff --check`：通过。
- 真实 rollout 回放：15 个含 MCP 的文件、37/37 调用唯一可见、37/37 名称精确、2/2 错误识别、37/37 duration 精确、25/25 三段序列只关联一次、0 个原始 arguments 被保留。

## 对抗性审查

- 直接 cherry-pick 会冲突并覆盖本地更强的 custom-tool/exec 解析，因此只移植必要行为和上游 runtime 测试。
- MCP 名称来自外部事件，现已去除终端与双向文本控制符并限制长度。
- duration 只接受安全整数秒和 `[0, 1_000_000_000)` 纳秒，异常值不显示。
- 跨增量重命名不能依赖 `recentCalls` 仍保留目标记录；实现追踪原运行对象，即使列表被截断也能迁移累计计数。
- 本机 `SHELL=/bin/zsh` 下的既有真实 tmux 测试受用户启动脚本影响；隔离 `/bin/sh` 后通过，未修改与本任务无关的 wrapper 或测试。
