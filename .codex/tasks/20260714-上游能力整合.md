# 上游能力整合

## 基线

- 本地基线：`main` / `4526d249a411604e976dc84a8ba42a722d10d0c6`
- 上游基线：`upstream/main` / `dede7b2cdf05d22458672f3b0cf4028bdcf04d53`
- 整合分支：`integrate/upstream-main-20260714`
- 策略：以本地 `main` 为唯一基线选择性移植，不直接 merge/reset 上游。

## 必须保留的本地行为

- detach/终端关闭后 tmux/Codex 会话可重连。
- Codex 退出后保留 tmux session 和 shell。
- HUD 会话内再次运行 `codex` 不嵌套 tmux。
- 保留基于 pane/process/logs/state/snapshot 的 root session 绑定。
- 保留 `/new`/`/resume` 快速切换、`node:sqlite` fallback。
- 保留 `exec_command`/`args.cmd` 目标展示。
- 保留任意 `src/**/*.ts[x]` 变更触发构建。
- 保留 installer/uninstaller 的 alias、fish、legacy cleanup 增强。

## 整合阶段

1. Context 计算、Expanded 去重、TMUX_PANE 格式、rollout canonical/call correlation。
2. Client-attach gate，保留本地生命周期语义。
3. Typed subagent collector、JSONL tail、renderer、overview 过滤。
4. README 同步和完整安全测试矩阵。
5. 最终 diff/不变量/剩余风险审查。

## 当前状态

- [x] 实时确认本地与上游基线。
- [x] 创建独立整合分支。
- [x] 完成代码整合。
- [x] 完成安全测试矩阵。
- [x] 完成最终审查。

## 验证记录

- `git fetch --prune upstream`：`upstream/main` 仍为 `dede7b2cdf05d22458672f3b0cf4028bdcf04d53`。
- `npm run build`：通过。
- `tests/unit/*.mjs`：全部通过。
- fake-tmux wrapper 集成：alternate-screen、resize、management、mouse、session attach、build staleness、client attach gate、main pane env 全部通过。
- `tests/integration/test-agent-activity-tree.mjs`：7/7 通过。
- `tests/e2e/test-agent-status-hud.mjs`：1/1 通过，清理后无测试子进程和临时目录残留。
- `bash -n bin/codex-hud tests/integration/test-wrapper-client-attach-gate.sh`：通过。
- `git diff --check`：通过。

## 真实 tmux 验证与风险边界

- 使用真实 `tmux 3.5a`、真实 PTY、真实 `codex-cli 0.144.4` 和独立 `TMUX_TMPDIR` 完成 attach/detach 测试。
- 首次 attach 后 gate 从 `0` 变为 `1`，真实 `codex --version` 执行并退出；随后 `Ctrl-b d` detach 返回码为 0。
- detach 后同一 session 保持 `attached=0`，主 pane 为存活 `zsh`，HUD pane 为存活 `node`；主 pane 成功执行 `__HUD_REAL_SHELL_ALIVE__` 标记。
- `codex-hud --attach` 成功复用同一 session，没有新建会话。
- 精确终止隔离 tmux attach client PID 模拟终端关闭后，session、主 pane 和 HUD pane 均保持存活；主 pane 成功执行 `__HUD_AFTER_CLIENT_TERM_ALIVE__` 标记，并可再次 attach/detach。
- 补充验证真实 attach gate：非 TTY 创建 session 后确认 gate=`0` 且尚无 `codex-cli` 输出；真实 PTY attach 后 gate=`1`，随后出现 `codex-cli 0.144.4`。
- 清理仅作用于隔离 server；测试前后默认 tmux server 均为相同的 8 个 session，隔离进程和临时目录残留均为 0。
- 仍未运行 `tests/test-e2e.sh`：脚本直接执行默认 socket 上的 `tmux kill-server`，会影响用户真实 tmux server；对应生命周期行为已由上述隔离真实测试覆盖。
- 未创建 Git commit；所有改动保留在 `integrate/upstream-main-20260714` 工作区供用户审阅。
