# 上游 v1.0 选择性整合

## 基线与目标

- 当前分支：`integrate/upstream-main-20260714`
- 当前提交：`675d125c70a1713d336f3254e961a4dd0e70f7f6`
- 上游比较基线：`dede7b2cdf05d22458672f3b0cf4028bdcf04d53`
- 上游目标：`upstream/main` / `90f167391e80625a25747d4950b0c65aabc1beb7` / `v1.0`
- 策略：以当前分支为唯一实现基线，按行为移植，不直接 merge、reset 或整文件接受上游版本。

## 必须保留的本地不变量

- detach、终端关闭后 tmux/Codex 会话可重连。
- Codex 退出后保留 tmux session 和 shell。
- HUD 会话内再次运行 `codex` 不嵌套 tmux。
- 保留 cmux workspace/surface/socket context 与 wrapper shim 传播。
- 保留 pane/process/log/state/snapshot/`process_uuid` SessionFinder、`/new`/`/resume` rebinding 和 sqlite fallback。
- 保留 `custom_tool_call` / `custom_tool_call_output` 兼容、保守工具名解析和 wait 展示规则。
- 保留广义 TypeScript rebuild staleness 检测、installer/uninstaller cleanup 与隔离测试约束。
- 不运行会影响默认 tmux server 的测试。

## 合并白名单

- [x] 运行时 Approval、Sandbox、Fast mode 状态解析和增量刷新。
- [x] skills/hooks 有效条目统计、去重和短缓存。
- [x] watcher 同步/异步错误可诊断化。
- [x] context-first token 行与运行时环境行。
- [x] 可读 tmux session 名称、默认 5 行高度、`Prefix+H` 改为 opt-in。
- [x] `cx` alias 与对应安装/卸载测试。
- [x] 上游新增能力的定向单元和 fake/隔离集成测试。

## 明确排除或独立决策

- [x] 嵌套 tmux attach：明确不合并；保留 HUD 内直接 exec、避免嵌套的现有策略。
- [x] 自动 updater：明确不合并；官方 Release 与 fork/定制跟踪分支拓扑不兼容。
- [x] 手动升级：改为针对当前 `@{upstream}` 的隔离构建、已跟踪文件复核、fast-forward 和产物回滚；不假定 `origin/main`。
- [x] 上游整文件版本的 `session-finder.ts`、`rollout.ts`、wrapper 和 installer：不接受，均基于本地版本局部移植。
- [x] README 截图与纯历史清理：不合并；只同步四种语言中本轮已落地的行为。

## 验证计划

- `npm run build`
- 全部 `tests/unit/*.mjs`
- 新增 runtime permission、Fast mode、assets、watcher 定向测试
- 既有 wrapper fake-tmux 测试与新增 session name/default height/toggle/alias 测试
- agent activity tree 与隔离 agent HUD E2E
- `bash -n`、`git diff --check`
- 最终检查本地不变量对应代码与测试仍存在

## 当前状态

- [x] 刷新并确认上游 `v1.0`。
- [x] 审查既有选择性整合记录和当前工作区。
- [x] 完成代码整合。
- [x] 完成安全测试矩阵。
- [x] 完成对抗性审查和差异收口。

## 验证结果

- `npm run build`：通过。
- `tests/unit/*.mjs`：18 个入口全部通过。
- `tests/integration/`：5 个 Node 入口和 14 个 shell 入口全部通过。
- `node tests/e2e/test-agent-status-hud.mjs`：16 个状态迁移全部在 5 秒阈值内，清理后无子进程或临时目录残留。
- `bash tests/test-e2e.sh`：10/10 通过；真实 `codex --help`、`codex --version` 和 wrapper `--list` 正常。
- 隔离 `HOME` 下的 `bash tests/test-install.sh`：10/10 通过。
- `node dist/test-render.js`、全部 shell `bash -n`、`git diff --check`：通过。
- 真实 rollout 只读抽样确认当前协议包含 `turn_context.approval_policy`、`sandbox_policy.type` 和 `thread_settings_applied.thread_settings.service_tier = null`；实现保留 `null` 为运行时 Fast off，不回退到陈旧静态配置。
- 事务式升级验证了安装失败、构建失败、候选构建修改 tracked 文件三类预构建失败均不移动 `HEAD`；成功路径支持非默认 `integrate/v1` 跟踪分支、远端修改 tracked dependency 的仓库拓扑，并保留不冲突的未跟踪文件；目标新增路径与本地未跟踪文件冲突时会在移动 `HEAD` 或产物前安全失败。
- `.codex/visualizations/` 为任务前已有未跟踪内容，本轮未修改。
- 实现阶段未自动创建 Git commit；随后用户明确授权提交本轮变更。未 push。
