{
  "name": "codex-hud",
  "version": "0.1.0",
  "description": "Real-time statusline HUD for OpenAI Codex CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "codex-hud": "./bin/codex-hud"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run typecheck && npm run build && bash tests/run-suite.sh unit && bash tests/run-suite.sh integration",
    "test:unit": "npm run build && bash tests/run-suite.sh unit",
    "test:integration": "npm run build && bash tests/run-suite.sh integration",
    "test:e2e:agent": "npm run build && node tests/e2e/test-agent-status-hud.mjs",
    "test:render": "npm run build && node dist/test-render.js"
  },
  "keywords": [
    "codex",
    "openai",
    "statusline",
    "hud",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "chokidar": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
